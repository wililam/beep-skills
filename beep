#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Beep · 小喇叭 - 一键集成验证
直接运行：python beep verify-integration
"""

import sys
from pathlib import Path

# 确保能导入包
package_dir = Path(__file__).parent / "audio_announcement"
if str(package_dir) not in sys.path:
    sys.path.insert(0, str(package_dir))

from audio_announcement.cli import main

if __name__ == "__main__":
    sys.exit(main())
